services:
  - type: web
    name: expense-tracker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn expense.wsgi
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: expense.settings
    autoDeploy: true
    migrateCommand: python manage.py migrate